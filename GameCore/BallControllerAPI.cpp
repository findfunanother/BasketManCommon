#include "pch.h"

#include "BallControllerAPI.h"

//CBallSimulator* ballSimulator;
//
//DECLSPEC CBallSimulator* Initialize()
//{
//	ballSimulator = new CBallSimulator();
//	ballSimulator->Initialize();
//
//	return ballSimulator;
//}
//
//DECLSPEC CBallSimulationCurve* MakeNewCurveFromTwoCurve(CBallSimulator* bs, float reachTime, string* addEvent, JOVECTOR3 startPos, JOVECTOR3 targetPos, JOVECTOR3 targetVelocity)
//{
//	return bs->MakeNewCurveFromTwoCurve(reachTime, *addEvent,
//		startPos, targetPos, targetVelocity);
//	/*return simulator->MakeNewCurveFromTwoCurve(reachTime, addEvent,
//		startPos, targetPos, targetVelocity, before, after, false);*/
//}
